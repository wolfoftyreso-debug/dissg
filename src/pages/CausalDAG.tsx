import React from 'react';
import { CausalGraphView } from '@/components/causal/CausalGraphView';

const CausalDAGPage: React.FC = () => {
  return <CausalGraphView />;
};

export default CausalDAGPage;
