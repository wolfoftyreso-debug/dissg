import React from 'react';
import { PerspectiveEngine } from '@/components/perspective';

const PerspectiveDemo: React.FC = () => {
  return <PerspectiveEngine />;
};

export default PerspectiveDemo;
