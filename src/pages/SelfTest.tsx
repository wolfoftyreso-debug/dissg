/**
 * Self-Test Page
 * 
 * Internal system diagnostics dashboard.
 */

import React from 'react';
import { SelfTestDashboard } from '@/components/diagnostic/SelfTestDashboard';

const SelfTestPage: React.FC = () => {
  return <SelfTestDashboard />;
};

export default SelfTestPage;
