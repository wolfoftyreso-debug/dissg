/**
 * ONTOLOGY — Core Object Model
 * 
 * Everything in the system is built on these primitives.
 */

export {
  createTruthNode,
  validateTruthNode,
  type TruthNode,
  type TruthNodeType,
  type TruthScope,
  type GeoScope,
  type PopulationScope,
  type TimeScope,
  type SemanticImportanceMarkers,
  type DataReference,
  type TruthRelations,
  type RelationLink,
  type RelationType,
  type TruthNodeMetadata,
} from './truth-node';
